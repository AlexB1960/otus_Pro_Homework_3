# Homework N3

Проводится API-тестирование метода GET /pet/findByStatus и метода POST /pet/findByStatus.
Используются спецификации RequestSpecification и ResponseSpecification, а также валидация
json-схем postpetschema.json и getpetschema.json.